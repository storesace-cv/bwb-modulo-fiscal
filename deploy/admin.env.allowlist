# Allowlist for /etc/bwb-modulo-fiscal/admin.env (root-only; never loaded by systemd).
# Helper reads as root, then env -i + drop-priv to bwb-fiscal-admin.
# Privileged DSN for scopes/credentials admin — not fiscal_runtime or fiscal_migrate.

FISCAL_DATABASE_DRIVER
FISCAL_DATABASE_URL

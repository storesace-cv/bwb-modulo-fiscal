# Provisional zone filename retained on hosts from S3A/S3B.
# S3C2 ships the final zone as bwb-limit-req-documents.conf (identical rate=10r/s).
# Include once under http { }. Final rate/burst approved by S3C1.

limit_req_zone $binary_remote_addr zone=bwb_documents:10m rate=10r/s;

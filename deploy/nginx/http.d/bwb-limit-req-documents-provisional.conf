# Provisional limit_req zone for S3A candidate + loopback measurement (identical).
# Include once under http { }. Final rate/burst are fixed in PR S3C after measurement.
# S3A values (hypothesis: multi-POS behind one NAT): 10r/s, burst 20.

limit_req_zone $binary_remote_addr zone=bwb_documents:10m rate=10r/s;

# Final limit_req zone for public /v1/documents (S3C2).
# Values approved by S3C1: rate=10r/s, burst=20 (identical to measurement).
# Include once under http { } (typically /etc/nginx/conf.d/).

limit_req_zone $binary_remote_addr zone=bwb_documents:10m rate=10r/s;

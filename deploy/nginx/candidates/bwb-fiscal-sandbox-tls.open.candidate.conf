# DEPRECATED path kept for historical reference.
# S3C2 canonical open site: ../open/bwb-fiscal-sandbox-tls.open.conf
# Do not install this candidate file. Helper rejects install-nginx-open / activate-open-candidate.

# See open/bwb-fiscal-sandbox-tls.open.conf (rate=10r/s burst=20, limit_req_status 429).

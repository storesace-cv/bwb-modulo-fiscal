# Allowlist for /etc/bwb-modulo-fiscal/migrate.env (operator / fiscal-migrate only).
# Never load this file into the API systemd unit.
# Privileged DSN for schema changes — not the runtime role.

FISCAL_DATABASE_DRIVER
FISCAL_DATABASE_URL
